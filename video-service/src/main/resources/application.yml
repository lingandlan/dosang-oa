server:
  port: 8085

spring:
  application:
    name: video-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER:localhost:8848}
      config:
        server-addr: ${NACOS_SERVER:localhost:8848}
        file-extension: yml

# Jitsi 配置
jitsi:
  url: ${JITSI_URL:https://meet.example.com}
  api-key: ${JITSI_API_KEY:}
